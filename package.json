{
  "name": "@deepseek-ai/dsh-extrapro-anchor",
  "description": "Seed every top-level session with one pre-sampled minimal-style virtual read turn whose tool result elevates the session to the preset's real prompt — deterministic trajectory anchoring without any real tool call, plus a collapsible floating Web panel (injection switch, chain-health readout, injected-text editor).",
  "version": "0.2.0",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./package.json": "./package.json",
    "./panel": "./panel/index.js",
    "./panel/package.json": "./panel/package.json",
    "./panel/*": "./panel/*"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "keywords": [
    "deepseek-harness",
    "dsh",
    "plugin",
    "dsh-plugin",
    "anchor",
    "trajectory",
    "minimal"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check lib/index.js && node --check lib/runtime.js && node --check lib/guards.js && node --check lib/settings.js && node --check lib/health.js && node --check lib/config-remote.js && node --check panel/index.js && node --check panel/client.js && node --test"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1"
  },
  "license": "MIT"
}
